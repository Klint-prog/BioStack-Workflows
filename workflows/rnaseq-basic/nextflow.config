profiles {
    local {
        process.executor = 'local'
    }

    docker {
        process.executor = 'local'
        docker.enabled = true
        process.container = 'ubuntu:22.04'
    }
}

process {
    errorStrategy = 'terminate'
}
